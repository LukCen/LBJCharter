import ChartDataForm from "@/components/ChartDataForm.vue";
import { createMemoryHistory, createRouter } from "vue-router";

const routes = [
  { path: '/', component: ChartDataForm },
  { path: '/upload', component: '' }
]
